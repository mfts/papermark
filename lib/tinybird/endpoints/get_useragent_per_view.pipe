VERSION 3

NODE endpoint
SQL >
    %
    SELECT
        country,
        city,
        browser,
        os,
        device
    FROM
        pm_click_events__v1
    WHERE
        viewId = {{ String(viewId, required=True) }}
    LIMIT 1
