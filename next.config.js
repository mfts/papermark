/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["darr6llpvnyot.cloudfront.net", "lh3.googleusercontent.com"],
  },
};

module.exports = nextConfig
